%Homework 1: 
for integer = 40:50
    square = integer^2
    fprintf('The square of %d is %d/n', integer, square);
end

%Homework 2

oddcounts = (1:2:1000) % this increases by 2 which gives odd numbers 

for trial = 1:ntrials 
 duration = length(data(trial).signalIn) / data(trial).params.sRate 
 fprintf('Trial %d was %d seconds long.\n',trial,duration)
end
 
%Homework 3: 
for trialIx = 1:length(data)
trialFileName = [num2str(1) '.mat'];
fprintf('The file name for trial %d will be %s/n', trialIx, trialFileName);
end 

%Homework 4
goodEvilAvailable = [1 4 12 18];
i = 1;
for i = 1:length(goodEvilAvailable)
    charIx = goodEvilAvailable(i); %you get the 1st, 2nd, 3rd and 4th value
    % in goodEvilAvailable and that value is used as an index 
    officeCharacters(charIx).goodEvil = goodEvil{i};
end 

%Homework 5
for trailIx = 1: length(words4trails)
    trailFileName = [num2str(1) '.mat'];
    fprintf('The word for trial TRIALNUMBER i
    