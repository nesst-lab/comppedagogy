1. The variables are now a=15, b=17, c=4. They changed because the new data that we entered into the function caused the original variables to be overwritten. 

2. vectorMin would return 1 as the value.
